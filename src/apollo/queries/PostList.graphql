query GetPostList(
  $options: PageQueryOptions
) {
  posts(options: $options) {
    data {
      id,
      title,
      user {
        id,
        username,
      }
    },
    meta {
      totalCount,
    },
  }
}
